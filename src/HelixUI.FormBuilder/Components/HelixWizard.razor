@namespace HelixUI.FormBuilder.Components
@inherits HelixUI.Core.Base.HelixComponentBase

<div class="@CssClass" @attributes="AdditionalAttributes">
    <nav class="helix-wizard__nav" role="tablist" aria-label="Form steps">
        @for (var i = 0; i < _steps.Count; i++)
        {
            var stepIndex = i;
            var step = _steps[i];
            var isCurrent = stepIndex == CurrentStep;
            var isCompleted = stepIndex < CurrentStep;
            var isAccessible = !Linear || stepIndex <= _highestReached;

            <button type="button"
                    class="helix-wizard__step-btn @(isCurrent ? "helix-wizard__step-btn--current" : "") @(isCompleted ? "helix-wizard__step-btn--completed" : "")"
                    role="tab"
                    aria-selected="@(isCurrent ? "true" : "false")"
                    aria-controls="@($"helix-wizard-panel-{stepIndex}")"
                    disabled="@(!isAccessible)"
                    @onclick="() => GoToStep(stepIndex)">
                <span class="helix-wizard__step-indicator">
                    @if (isCompleted) { <span aria-hidden="true">✓</span> }
                    else { @(stepIndex + 1) }
                </span>
                <span class="helix-wizard__step-title">@step.Title</span>
            </button>
        }
    </nav>

    @if (ShowProgress)
    {
        <div class="helix-wizard__progress" role="progressbar"
             aria-valuenow="@ProgressPercent" aria-valuemin="0" aria-valuemax="100"
             aria-label="@($"Step {CurrentStep + 1} of {_steps.Count}")">
            <div class="helix-wizard__progress-bar" style="width: @(ProgressPercent)%;"></div>
        </div>
    }

    <div class="helix-wizard__panel"
         id="@($"helix-wizard-panel-{CurrentStep}")"
         role="tabpanel">
        <CascadingValue Value="this" IsFixed="true">
            @ChildContent
        </CascadingValue>
    </div>

    <div class="helix-wizard__actions">
        @if (CurrentStep > 0)
        {
            <button type="button" class="helix-wizard__btn helix-wizard__btn--prev" @onclick="Previous">
                @PreviousText
            </button>
        }
        <div class="helix-wizard__actions-spacer"></div>
        @if (CurrentStep < _steps.Count - 1)
        {
            <button type="button" class="helix-wizard__btn helix-wizard__btn--next" @onclick="Next">
                @NextText
            </button>
        }
        else
        {
            <button type="button" class="helix-wizard__btn helix-wizard__btn--complete" @onclick="Complete">
                @CompleteText
            </button>
        }
    </div>
</div>
