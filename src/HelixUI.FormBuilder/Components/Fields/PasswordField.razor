@namespace HelixUI.FormBuilder.Components.Fields
@inherits FieldBase

<div class="@ContainerClass">
    <label class="helix-field__label" for="@ElementId">
        @EffectiveLabel
        @if (EffectiveRequired) { <span class="helix-field__required" aria-hidden="true">*</span> }
    </label>

    <div class="helix-field__password-wrapper">
        <input id="@ElementId"
               type="@(_visible ? "text" : "password")"
               class="helix-field__input helix-field__password-input"
               value="@Value"
               placeholder="@EffectivePlaceholder"
               disabled="@EffectiveDisabled"
               readonly="@ReadOnly"
               required="@EffectiveRequired"
               autocomplete="@(IsNew ? "new-password" : "current-password")"
               aria-required="@(EffectiveRequired ? "true" : null)"
               aria-invalid="@(HasErrors ? "true" : null)"
               aria-describedby="@AriaDescribedBy"
               @oninput="HandleInput"
               @attributes="AdditionalAttributes" />
        <button type="button"
                class="helix-field__password-toggle"
                aria-label="@(_visible ? "Hide password" : "Show password")"
                @onclick="ToggleVisibility">
            @(_visible ? "Hide" : "Show")
        </button>
    </div>

    @if (ShowStrength && !string.IsNullOrEmpty(Value))
    {
        <div class="helix-field__password-strength">
            <div class="helix-field__password-strength-bar" style="width: @(StrengthPercent)%; background: @StrengthColor;"></div>
        </div>
        <div class="helix-field__helper">@StrengthLabel</div>
    }

    @if (!string.IsNullOrEmpty(EffectiveHelperText) && (string.IsNullOrEmpty(Value) || !ShowStrength))
    {
        <div id="@HelperId" class="helix-field__helper">@EffectiveHelperText</div>
    }

    @if (HasErrors)
    {
        <div id="@ErrorId" class="helix-field__errors" role="alert">
            @foreach (var error in Errors!)
            {
                <div class="helix-field__error">@error</div>
            }
        </div>
    }
</div>

@code {
    [Parameter] public string? Value { get; set; }
    [Parameter] public EventCallback<string?> ValueChanged { get; set; }
    [Parameter] public bool ShowStrength { get; set; } = true;
    [Parameter] public bool IsNew { get; set; } = true;

    private bool _visible;

    private void ToggleVisibility() => _visible = !_visible;

    private async Task HandleInput(ChangeEventArgs e)
    {
        Value = e.Value?.ToString();
        await ValueChanged.InvokeAsync(Value);
    }

    private int StrengthScore
    {
        get
        {
            if (string.IsNullOrEmpty(Value)) return 0;
            var score = 0;
            if (Value.Length >= 8) score++;
            if (Value.Length >= 12) score++;
            if (System.Text.RegularExpressions.Regex.IsMatch(Value, @"[a-z]") &&
                System.Text.RegularExpressions.Regex.IsMatch(Value, @"[A-Z]")) score++;
            if (System.Text.RegularExpressions.Regex.IsMatch(Value, @"\d")) score++;
            if (System.Text.RegularExpressions.Regex.IsMatch(Value, @"[^a-zA-Z\d]")) score++;
            return score;
        }
    }

    private int StrengthPercent => StrengthScore * 20;
    private string StrengthColor => StrengthScore switch
    {
        <= 1 => "var(--helix-color-danger)",
        2 => "var(--helix-color-warning)",
        3 => "var(--helix-color-warning)",
        4 => "var(--helix-color-success)",
        _ => "var(--helix-color-success)"
    };
    private string StrengthLabel => StrengthScore switch
    {
        <= 1 => "Weak",
        2 => "Fair",
        3 => "Good",
        4 => "Strong",
        _ => "Very strong"
    };
}
